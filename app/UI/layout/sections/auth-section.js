export const AuthSection = ({ children }) => {
  return (
    <section className="bg-blackTheme min-h-screen flex flex-col md:relative justify-center items-center">
      {children}
    </section>
  );
};
