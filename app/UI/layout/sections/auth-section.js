export const AuthSection = ({ children }) => {
  return (
    <section className="bg-light-gray min-h-screen flex flex-col md:relative justify-center items-center">
      {children}
    </section>
  );
};
