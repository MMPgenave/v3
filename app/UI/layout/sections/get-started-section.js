export const GetStartedSection = ({ children }) => {
  return (
    <section className="bg-light-gray p-4 flex flex-col items-center justify-center min-h-screen">
      {children}
    </section>
  );
};
